#include "../include/LinkedList.hpp"
 
int main()
{
//测试两种插入建表方式
    // List<int> lf, lr;
    // lf.inputFront(0);//以0为结束标志
    // lr.inputRear(0);//以0为结束标志

    // cout << "lf的长度为：" << lf.Length() << endl;
    // cout << "lr的长度为：" << lr.Length() << endl;
    // cout << endl;

    // cout << "lf的内容为：" << endl;
    // lf.output();
    // cout << "lr的内容为：" << endl;
    // lr.output();
    // cout << endl;


//测试列表初始化
    // List<int> l1{1, 2, 3};
    // cout << "l1的长度为：" << l1.Length() << endl;
    // l1.output();


    return 0;
}